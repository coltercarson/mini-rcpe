# Mini-RCPE Configuration

# Admin password for recipe editing
ADMIN_PASSWORD=changeme

# Port to expose the application on
APP_PORT=8000

# Database file path (inside container)
DB_PATH=/app/data/rcpe.db

# Local LLM Configuration (optional)
# Set to "true" to enable LLM fallback when recipe-scrapers fails
LLM_ENABLED=false

# Ollama API endpoint (default: http://localhost:11434)
LLM_BASE_URL=http://localhost:11434

# Model to use for recipe extraction (default: llama3.2)
# Other options: llama3.2:1b, mistral, llama2, etc.
LLM_MODEL=llama3.2

# Timeout for LLM requests in seconds (default: 120)
LLM_TIMEOUT=120
